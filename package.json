{
  "name": "jubro",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build:css && npm run copy:fontawesome",
    "build:css": "tailwindcss -i ./css/tailwind-input.css -o ./css/tailwind.css --minify",
    "copy:fontawesome": "node ./scripts/copy-fontawesome.mjs"
  },
  "devDependencies": {
    "@fortawesome/fontawesome-free": "^6.5.0",
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
